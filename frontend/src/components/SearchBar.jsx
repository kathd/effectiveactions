import React from "react";
import { Input } from "semantic-ui-react";

const SearchBar = () => (
  <Input
    icon="search"
    iconPosition="left"
    placeholder="Search..."
    className="searchbar"
  />
);

export default SearchBar;
